# local working notes (plan-folder convention — never committed)
plan/

# OS junk
.DS_Store
Thumbs.db
._*
.Spotlight-V100
.Trashes

# Python / uv (export + train tooling)
.venv/
.venv-onnx/
__pycache__/
*.pyc

# node (browser harness)
node_modules/

# T6 custom-WGSL kernel backend — a fork of tylerstraub/gemma4-webgpu with its
# own git history (branch qwen3-spike). Tracked there, not in the lab repo.
custom-kernels/

# heavy model artifacts (exports live on HF / the Studio, not git)
*.onnx
*.onnx_data
onnx-out/
onnx-sym/
fused/
fused-hf/
*.gguf
*.safetensors

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
.env*
!.env.example
